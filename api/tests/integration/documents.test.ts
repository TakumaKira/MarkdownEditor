import { API_PATHS } from "../../src/constants"

describe(`POST ${API_PATHS.DOCUMENTS.path}`, () => {
  // authApiMiddleware tests
  test('returns 401 without updating any documents if no auth token provided', async () => {

  })

  test('returns 401 without updating any documents if no auth token is not valid', async () => {

  })

  test('returns 401 without updating any documents if no auth token is not encoded with right secret', async () => {

  })

  test('returns 401 without updating any documents if no auth token is not authToken', async () => {

  })

  // Test long name

  // Test long contents

  test('', async () => {

  })
})
