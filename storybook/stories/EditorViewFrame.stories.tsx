import { storiesOf } from '@storybook/react-native';
import EditorViewFrame from '../../components/EditorViewFrame';

storiesOf('EditorViewFrame', module).add('to Storybook', () =>
  <EditorViewFrame />
);
