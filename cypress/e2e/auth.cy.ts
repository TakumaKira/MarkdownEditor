describe('auth' , () => {
  context('singup', () => {
    it('passes', () => {
      cy.visit('/')
    })
  })

  context('login', () => {

  })

  context('edit account', () => {

  })

  context('delete account', () => {

  })
})
